# Environment configuration for Recipe Explorer frontend (React CRA)

# Keep mocks enabled by default during development.
# Set to "true" to use local in-app mocks; set to "false" to call a backend API.
REACT_APP_USE_MOCKS=true

# Base URL of the backend API when REACT_APP_USE_MOCKS=false.
# Examples:
#   http://localhost:8080
#   https://api.example.com
REACT_APP_API_BASE_URL=http://localhost:8080
